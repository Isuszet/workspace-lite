# 🚀 Инструкция по установке и запуску Workspace Lite

## Пошаговая установка

### Шаг 1: Создание структуры проекта

Создайте следующую структуру папок:

```
workspace-lite/
├── electron/
│   ├── main.ts
│   ├── preload.ts
│   └── database.ts
├── src/
│   ├── components/
│   │   ├── Sidebar.tsx
│   │   ├── PageList.tsx
│   │   ├── Editor.tsx
│   │   ├── MarkdownEditor.tsx
│   │   └── SearchModal.tsx
│   ├── types/
│   │   └── index.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tsconfig.electron.json
├── tailwind.config.js
├── postcss.config.js
├── .gitignore
└── README.md
```

### Шаг 2: Скопируйте все файлы

Скопируйте содержимое всех файлов из артефактов в соответствующие файлы в вашем проекте.

### Шаг 3: Установка зависимостей

```bash
cd workspace-lite
npm install
```

### Шаг 4: Первый запуск

**Вариант А: Только фронтенд (для разработки UI)**
```bash
npm run dev
```
Откроется в браузере на http://localhost:5173

**Вариант Б: Полное Electron приложение (рекомендуется)**
```bash
npm run electron:dev
```
Откроется desktop-приложение

### Шаг 5: Проверка работоспособности

После запуска вы должны увидеть:
- ✅ Левую панель с навигацией
- ✅ Две демо-страницы: приветственная инструкция и пример задачи
- ✅ Центральную панель со списком страниц
- ✅ Правую панель с редактором

**Тесты:**
1. Нажмите Ctrl+K — откроется поиск
2. Нажмите Ctrl+N — создастся новая заметка
3. Кликните на задачу, измените статус
4. Попробуйте экспорт в Markdown

## Сборка для продакшена

### Подготовка

Убедитесь, что все работает в dev режиме.

### Сборка

```bash
# Полная сборка с инсталлятором
npm run dist
```

Результат появится в папке `release/`:
- **Windows**: `workspace-lite Setup X.X.X.exe` и `.exe` (portable)
- **macOS**: `workspace-lite-X.X.X.dmg` и `.zip`
- **Linux**: `workspace-lite-X.X.X.AppImage` и `.deb`

## Устранение проблем

### Ошибка: "Cannot find module 'better-sqlite3'"

```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Ошибка: "Failed to fetch dynamically imported module"

Это нормально в dev режиме при первом запуске. Просто перезагрузите приложение.

### Electron окно не открывается

```bash
# Проверьте, что Vite dev server запущен
npm run dev

# В другом терминале
npx electron .
```

### База данных не создаётся

Проверьте права на запись в директорию:
- Windows: `%APPDATA%`
- macOS: `~/Library/Application Support`
- Linux: `~/.config`

## Дополнительные настройки

### Изменение порта Vite

В `vite.config.ts`:
```typescript
server: {
  port: 5173, // измените на нужный
}
```

### Кастомизация иконки приложения

1. Создайте иконки:
   - Windows: `icon.ico` (256x256)
   - macOS: `icon.icns` 
   - Linux: `icon.png` (512x512)

2. Добавьте в `package.json` секцию `build`:
```json
"build": {
  "win": {
    "icon": "build/icon.ico"
  },
  "mac": {
    "icon": "build/icon.icns"
  },
  "linux": {
    "icon": "build/icon.png"
  }
}
```

### Включение автообновлений

Добавьте в `package.json`:
```json
"build": {
  "publish": {
    "provider": "github",
    "owner": "your-username",
    "repo": "workspace-lite"
  }
}
```

## Системные требования

### Минимальные
- **OS**: Windows 10 / macOS 10.14 / Ubuntu 18.04
- **RAM**: 4 GB
- **Диск**: 500 MB свободного места
- **Node.js**: 18.x или выше (только для разработки)

### Рекомендуемые
- **OS**: Windows 11 / macOS 12+ / Ubuntu 22.04
- **RAM**: 8 GB
- **Диск**: 1 GB свободного места
- **Node.js**: 20.x (только для разработки)

## Полезные команды

```bash
# Очистка сборки
rm -rf dist dist-electron release

# Проверка TypeScript
npx tsc --noEmit

# Форматирование кода (если используете Prettier)
npx prettier --write "src/**/*.{ts,tsx}"

# Проверка размера сборки
npm run build
du -sh dist/

# Тестовый запуск собранного приложения
npm run package
./release/linux-unpacked/workspace-lite  # Linux
./release/mac/workspace-lite.app         # macOS
./release/win-unpacked/workspace-lite.exe # Windows
```

## Что дальше?

После успешного запуска:

1. ✅ Изучите demo-данные
2. ✅ Создайте свою первую заметку
3. ✅ Попробуйте все горячие клавиши
4. ✅ Протестируйте экспорт
5. ✅ Настройте под свои нужды

Приятной работы! 🎉