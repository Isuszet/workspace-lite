# 🚀 Как запустить приложение

## Вариант 1: Автоматический запуск (рекомендуется) ✅

**Одна команда - всё запускается автоматически!**

```bash
npm run electron:dev
```

Эта команда:
1. ✅ Компилирует TypeScript файлы Electron
2. ✅ Запускает Vite dev server (порт 5173)
3. ✅ Ждет готовности Vite
4. ✅ Запускает Electron приложение

**Всё в одной консоли!** Используется `concurrently` для параллельного запуска процессов.

---

## Вариант 2: Ручной запуск (2 консоли) 🔧

Если хотите видеть логи раздельно или есть проблемы с автоматическим запуском:

### Консоль 1: Vite Dev Server

```bash
# 1. Сначала скомпилируйте Electron файлы
npm run electron:compile

# 2. Запустите Vite dev server
npm run dev
```

Вы увидите:
```
VITE v5.4.21  ready in 878 ms
➜  Local:   http://127.0.0.1:5173/
```

### Консоль 2: Electron приложение

**После того, как Vite запустился**, в другой консоли:

```bash
npm run electron:start
```

Или если хотите скомпилировать заново:

```bash
npm run electron:compile
npm run electron:start
```

---

## Порядок запуска (детально)

### Шаг 1: Компиляция Electron файлов

```bash
npm run electron:compile
```

**Что происходит:**
- Компилирует `electron/*.ts` → `dist-electron/electron/*.cjs`
- Переименовывает `.js` → `.cjs` для совместимости
- Исправляет пути импортов

**Результат:**
```
✓ Переименован: main.js → main.cjs
✓ Переименован: preload.js → preload.cjs
✓ Переименован: database.js → database.cjs
✓ Обновлены пути к импортам в .cjs файлах
Готово!
```

### Шаг 2: Запуск Vite Dev Server

```bash
npm run dev
```

**Что происходит:**
- Запускает Vite dev server на `http://127.0.0.1:5173`
- Компилирует React приложение на лету
- Hot Module Replacement (HMR) для быстрой разработки

**Результат:**
```
VITE v5.4.21  ready in 878 ms
➜  Local:   http://127.0.0.1:5173/
```

### Шаг 3: Запуск Electron

```bash
npm run electron:start
```

**Что происходит:**
- Запускает Electron main process
- Загружает `dist-electron/electron/main.cjs`
- Инициализирует базу данных
- Создает окно и загружает `http://127.0.0.1:5173`

---

## Все доступные команды

### Разработка

| Команда | Описание |
|---------|----------|
| `npm run electron:dev` | **Автоматический запуск** - всё в одной команде |
| `npm run electron:dev:manual` | Только компиляция Electron файлов |
| `npm run electron:dev:simple` | Компиляция + Vite (без Electron) |
| `npm run dev` | Только Vite dev server |
| `npm run electron:compile` | Компиляция Electron TypeScript |
| `npm run electron:start` | Запуск Electron (нужен запущенный Vite) |

### Сборка

| Команда | Описание |
|---------|----------|
| `npm run build` | Сборка React приложения |
| `npm run electron:build` | Полная сборка для production |
| `npm run package` | Сборка без установщика (для тестирования) |
| `npm run dist` | Создание дистрибутива |

---

## Устранение проблем

### Проблема: "Vite не запустился"

**Решение:**
```bash
# Проверьте, что порт 5173 свободен
# Или измените порт в vite.config.ts
```

### Проблема: "Electron не может подключиться к Vite"

**Решение:**
1. Убедитесь, что Vite запущен и показывает `ready`
2. Проверьте URL в консоли Electron
3. Попробуйте перезапустить оба процесса

### Проблема: "Ошибки компиляции TypeScript"

**Решение:**
```bash
# Очистите и перекомпилируйте
npm run electron:compile
```

### Проблема: "База данных не инициализируется"

**Решение:**
- Проверьте права доступа к папке `userData`
- Путь: `%APPDATA%\workspace-lite\` (Windows)
- Удалите `workspace.db` для пересоздания

---

## Рекомендуемый workflow

### Для ежедневной разработки:

```bash
# 1. Откройте проект
cd workspace-lite

# 2. Запустите одной командой
npm run electron:dev
```

**Всё!** Приложение откроется автоматически.

### При изменении Electron файлов:

1. Остановите приложение (Ctrl+C)
2. Запустите снова:
   ```bash
   npm run electron:dev
   ```

### При изменении React компонентов:

- **Не нужно перезапускать!** Vite HMR обновит изменения автоматически
- Просто сохраните файл и увидите изменения в приложении

---

## Структура процессов

```
npm run electron:dev
│
├─ npm run electron:compile
│  └─ tsc -p tsconfig.electron.json
│  └─ node scripts/fix-electron-extensions.cjs
│
└─ concurrently
   ├─ npm run dev (Vite)
   │  └─ vite → http://127.0.0.1:5173
   │
   └─ npm run electron:wait
      └─ wait-on http://127.0.0.1:5173
      └─ electron . (после готовности Vite)
```

---

## Быстрая справка

### Одна команда для всего:
```bash
npm run electron:dev
```

### Две консоли (если нужно):
```bash
# Консоль 1:
npm run electron:compile
npm run dev

# Консоль 2 (после запуска Vite):
npm run electron:start
```

### Остановка:
- Нажмите `Ctrl+C` в консоли
- Или закройте окно Electron

---

## Что происходит при запуске

1. **Компиляция** (если нужно)
   - TypeScript → JavaScript
   - Исправление расширений файлов

2. **Vite Dev Server**
   - Запуск на порту 5173
   - Готовность к приему запросов

3. **Electron**
   - Инициализация БД
   - Создание окна
   - Загрузка `http://127.0.0.1:5173`
   - Отображение приложения

---

## Советы

✅ **Используйте `npm run electron:dev`** - это самый простой способ

✅ **Не закрывайте консоль** - она показывает логи и ошибки

✅ **При изменении Electron кода** - перезапустите приложение

✅ **При изменении React кода** - изменения применяются автоматически (HMR)

✅ **Для отладки** - DevTools открываются автоматически в режиме разработки
