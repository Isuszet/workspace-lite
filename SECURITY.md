# 🔒 Безопасность Workspace Lite

## Обзор безопасности

Workspace Lite разработан с акцентом на безопасность и конфиденциальность данных. Все данные хранятся локально и никогда не передаются на внешние серверы.

## Принципы безопасности

### 1. Offline-First

✅ **Никаких сетевых запросов**
- Приложение работает полностью офлайн
- Нет телеметрии или аналитики
- Нет отправки данных на сторонние серверы

✅ **Локальное хранение**
- Все данные в SQLite базе на вашем компьютере
- Вы контролируете, где хранятся файлы
- Легко создавать резервные копии

### 2. Electron Security

✅ **Context Isolation**
```javascript
webPreferences: {
  contextIsolation: true,  // ✅ Включено
  nodeIntegration: false,  // ✅ Отключено
  sandbox: true            // ✅ Рекомендуется
}
```

✅ **Preload Security**
- Минимальный API через contextBridge
- Только явно разрешенные операции
- Нет прямого доступа к Node.js из renderer

✅ **No Eval**
- Не используется eval() или подобные функции
- Content Security Policy (рекомендуется добавить)

### 3. Данные пользователя

✅ **Шифрование (опционально)**
Вы можете включить шифрование базы данных:

```typescript
// Установите SQLCipher
npm install @journeyapps/sqlcipher

// В database.ts
db.pragma(`key = "${yourPassword}"`);
```

✅ **Права доступа**
- База данных создаётся с правами только для текущего пользователя
- Нет доступа из других учетных записей (по умолчанию ОС)

## Известные ограничения безопасности

### 1. Шифрование базы данных

❌ **Текущее состояние:** База данных НЕ зашифрована по умолчанию

**Риск:** Если кто-то получит физический доступ к вашему компьютеру, он сможет прочитать базу данных.

**Решение:** 
- Используйте шифрование диска (BitLocker, FileVault)
- Или добавьте SQLCipher (см. CUSTOMIZATION.md)

### 2. Экспорт данных

❌ **Текущее состояние:** Экспортированные файлы не шифруются

**Риск:** Экспортированные MD/CSV файлы хранятся в открытом виде.

**Решение:**
- Храните экспорты в зашифрованных папках
- Удаляйте экспорты после использования

### 3. Резервные копии

❌ **Текущее состояние:** Нет автоматического резервного копирования

**Риск:** Потеря данных при сбое диска.

**Решение:**
- Регулярно копируйте workspace.db вручную
- Используйте облачное хранилище (OneDrive, iCloud) для папки данных

## Рекомендации по безопасности

### Для личного использования

1. ✅ **Используйте шифрование диска**
   - Windows: BitLocker
   - macOS: FileVault
   - Linux: LUKS

2. ✅ **Регулярные бэкапы**
   ```bash
   # Еженедельно копируйте БД
   cp workspace.db ~/backup/workspace-$(date +%Y%m%d).db
   ```

3. ✅ **Обновляйте приложение**
   - Следите за новыми версиями
   - Обновляйте при обнаружении уязвимостей

### Для командного использования

1. ✅ **Ограничьте доступ к базе**
   - Не храните конфиденциальные данные
   - Или используйте шифрование SQLCipher

2. ✅ **Политика экспорта**
   - Не экспортируйте конфиденциальные данные
   - Удаляйте экспорты после использования

3. ✅ **Аудит данных**
   - Регулярно проверяйте, что хранится в базе
   - Удаляйте устаревшие/ненужные страницы

## Отчёт об уязвимостях

### Как сообщить об уязвимости

Если вы обнаружили уязвимость в безопасности:

1. **НЕ создавайте публичный Issue**
2. Отправьте email на: [ваш email для security]
3. Укажите:
   - Описание уязвимости
   - Шаги для воспроизведения
   - Потенциальное влияние
   - Предложения по исправлению (если есть)

### Что мы гарантируем

- ✅ Ответ в течение 48 часов
- ✅ Исправление критических уязвимостей в течение 7 дней
- ✅ Публикация патча и advisory после исправления
- ✅ Упоминание исследователя (если он согласен)

### Severity уровни

| Уровень | Описание | SLA |
|---------|----------|-----|
| 🔴 Critical | Удаленное выполнение кода, утечка данных | 24 часа |
| 🟠 High | Локальное повышение привилегий | 7 дней |
| 🟡 Medium | DoS, раскрытие информации | 14 дней |
| 🟢 Low | Минорные проблемы | 30 дней |

## Аудит безопасности

### Последний аудит

- **Дата:** Не проводился (MVP версия)
- **Рекомендация:** Провести аудит перед версией 2.0

### Self-Assessment Checklist

- [x] Context Isolation включен
- [x] Node Integration отключен
- [x] Минимальный preload API
- [x] Нет eval() или Function()
- [x] Нет сетевых запросов
- [ ] Content Security Policy (TODO)
- [ ] Code Signing (TODO для production)
- [ ] Automated security tests (TODO)

## Зависимости

### Проверка уязвимостей

```bash
# Регулярно проверяйте зависимости
npm audit

# Исправление автоматически (с осторожностью)
npm audit fix

# Проверка outdated пакетов
npm outdated
```

### Критические зависимости

| Пакет | Версия | Статус |
|-------|--------|--------|
| electron | ^28.1.0 | ✅ Актуально |
| better-sqlite3 | ^9.2.2 | ✅ Актуально |
| react | ^18.2.0 | ✅ Актуально |

**Рекомендация:** Обновляйте зависимости ежемесячно.

## Compliance

### GDPR

✅ **Workspace Lite соответствует GDPR**, так как:
- Не собирает персональные данные
- Не передаёт данные третьим лицам
- Пользователь контролирует свои данные
- Легко удалить все данные (удалить файл БД)

### Право на забвение

✅ **Как удалить все данные:**

```bash
# Windows
del %APPDATA%\workspace-lite\workspace.db

# macOS/Linux
rm ~/Library/Application\ Support/workspace-lite/workspace.db
# или
rm ~/.config/workspace-lite/workspace.db
```

## Будущие улучшения безопасности

### Версия 1.1
- [ ] Content Security Policy
- [ ] Автоматические бэкапы
- [ ] Опциональное шифрование БД из UI

### Версия 1.2
- [ ] Code Signing для всех платформ
- [ ] Sandboxing mode
- [ ] Security audit report

### Версия 2.0
- [ ] End-to-end encryption для синхронизации
- [ ] Zero-knowledge architecture
- [ ] Security headers для web версии

## Best Practices для разработчиков

### При добавлении новых функций

1. ✅ **Никогда не добавляйте сетевые запросы** без явного согласия пользователя
2. ✅ **Минимизируйте API в preload** - только необходимые операции
3. ✅ **Валидируйте все входные данные** - не доверяйте пользовательскому вводу
4. ✅ **Используйте parameterized queries** - защита от SQL injection
5. ✅ **Не храните пароли** в plain text

### SQL Injection Prevention

```typescript
// ✅ ПРАВИЛЬНО - Prepared statements
db.prepare('SELECT * FROM pages WHERE id = ?').get(id);

// ❌ НЕПРАВИЛЬНО - String concatenation
db.prepare(`SELECT * FROM pages WHERE id = '${id}'`).get();
```

### XSS Prevention

```tsx
// ✅ ПРАВИЛЬНО - React автоматически экранирует
<div>{userContent}</div>

// ❌ ОПАСНО - dangerouslySetInnerHTML без санитизации
<div dangerouslySetInnerHTML={{ __html: userContent }} />
```

## Заключение

Workspace Lite стремится быть максимально безопасным приложением для хранения ваших данных. Мы придерживаемся принципа "privacy by design" и постоянно работаем над улучшением безопасности.

**Помните:** 
- ✅ Ваши данные хранятся только на вашем компьютере
- ✅ Никакой телеметрии или аналитики
- ✅ Полный контроль над вашими данными

---

**Вопросы по безопасности?** Свяжитесь с нами через Issues или email.

*Последнее обновление: 2024-01-16*