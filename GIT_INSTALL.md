# 📥 Установка Git для Windows

## Вариант 1: Официальный установщик (рекомендуется)

### Шаг 1: Скачайте Git

1. Перейдите на [git-scm.com/download/win](https://git-scm.com/download/win)
2. Скачайте установщик (автоматически выберется версия для вашей системы)
3. Или прямые ссылки:
   - **64-bit**: https://github.com/git-for-windows/git/releases/download/v2.43.0.windows.1/Git-2.43.0-64-bit.exe
   - **32-bit**: https://github.com/git-for-windows/git/releases/download/v2.43.0.windows.1/Git-2.43.0-32-bit.exe

### Шаг 2: Установите Git

1. Запустите скачанный `.exe` файл
2. Следуйте инструкциям установщика:
   - **Важно**: Оставьте все настройки по умолчанию
   - Особенно важно: выберите **"Git from the command line and also from 3rd-party software"**
   - Нажмите **"Next"** на всех шагах
3. Дождитесь завершения установки

### Шаг 3: Проверьте установку

1. **Закройте все окна терминала/PowerShell** (если были открыты)
2. Откройте **новый** PowerShell или CMD
3. Выполните:
   ```bash
   git --version
   ```
4. Должно появиться что-то вроде: `git version 2.43.0.windows.1`

✅ **Готово!** Git установлен.

---

## Вариант 2: Через winget (если установлен)

```powershell
winget install --id Git.Git -e --source winget
```

---

## Вариант 3: Через Chocolatey (если установлен)

```powershell
choco install git
```

---

## После установки Git

### Настройте Git (первый раз)

```bash
# Укажите ваше имя (замените на ваше)
git config --global user.name "Ваше Имя"

# Укажите ваш email (замените на ваш)
git config --global user.email "your.email@example.com"
```

### Проверьте настройки

```bash
git config --list
```

---

## Теперь можно загружать проект на GitHub!

После установки Git, вернитесь к инструкции [DEPLOY.md](DEPLOY.md) или [GITHUB_SETUP.md](GITHUB_SETUP.md) и выполните команды для загрузки проекта.

---

## ❓ Проблемы?

### Git все еще не работает после установки

1. **Закройте все окна терминала/PowerShell**
2. Откройте **новое** окно PowerShell или CMD
3. Проверьте переменную PATH:
   ```powershell
   $env:PATH -split ';' | Select-String git
   ```
4. Если Git не найден, перезагрузите компьютер

### Нужна помощь?

- Официальная документация: https://git-scm.com/doc
- Справка по установке: https://git-scm.com/book/en/v2/Getting-Started-Installing-Git
