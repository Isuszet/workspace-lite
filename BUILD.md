# 🔨 Сборка установщика Workspace Lite

## Быстрая сборка

### Для всех платформ

```bash
npm run dist
```

**Результат:** Установщики появятся в папке `release/`

---

## Детальная инструкция

### Шаг 1: Подготовка

```bash
# Убедитесь, что все зависимости установлены
npm install

# Проверьте, что проект компилируется
npm run build
```

### Шаг 2: Сборка установщика

```bash
# Полная сборка с установщиками
npm run dist
```

**Что происходит:**
1. Компилируется React приложение (`npm run build`)
2. Компилируется Electron код
3. Создаются установщики для всех платформ (или текущей)

### Шаг 3: Результаты

После сборки в папке `release/` появятся:

#### Windows
- `Workspace Lite Setup 1.0.0.exe` - NSIS установщик
  - ✅ Создает ярлык на рабочем столе
  - ✅ Создает ярлык в меню "Пуск"
  - ✅ Регистрируется в "Установка и удаление программ"
- `Workspace Lite-1.0.0-x64.exe` - Portable версия

#### macOS
- `Workspace Lite-1.0.0.dmg` - DMG образ для установки
- `Workspace Lite-1.0.0-mac.zip` - ZIP архив

#### Linux
- `Workspace Lite-1.0.0-x64.AppImage` - AppImage (запуск без установки)
- `Workspace Lite_1.0.0_amd64.deb` - DEB пакет для Ubuntu/Debian

---

## Иконки (опционально)

### Без иконок

**Можно собрать без иконок!** electron-builder создаст стандартные иконки Electron.

### С иконками

1. Подготовьте иконки:
   - **Windows**: `build/icon.ico` (256x256, многоразмерная)
   - **macOS**: `build/icon.icns`
   - **Linux**: `build/icon.png` (512x512 или 1024x1024)

2. Разместите в папке `build/`

3. Соберите: `npm run dist`

📖 **Подробнее:** см. `build/README.md`

---

## Сборка для конкретной платформы

### Только Windows

```bash
# На Windows
npm run dist
```

Или через скрипт:
```bash
npm run build:win
```

### Только macOS

```bash
# На macOS
npm run dist
```

Или через скрипт:
```bash
npm run build:mac
```

### Только Linux

```bash
# На Linux
npm run dist
```

Или через скрипт:
```bash
npm run build:linux
```

---

## Проверка установщика

### Windows

1. Запустите `Workspace Lite Setup 1.0.0.exe`
2. Следуйте инструкциям установщика
3. Проверьте:
   - ✅ Ярлык на рабочем столе
   - ✅ Ярлык в меню "Пуск"
   - ✅ Приложение запускается через ярлык

### macOS

1. Откройте `Workspace Lite-1.0.0.dmg`
2. Перетащите приложение в Applications
3. Проверьте:
   - ✅ Приложение в Launchpad
   - ✅ Можно закрепить в Dock

### Linux

1. Для AppImage: `chmod +x` и запустите
2. Для DEB: `sudo dpkg -i` или двойной клик
3. Проверьте:
   - ✅ Ярлык в меню приложений
   - ✅ Приложение запускается

---

## Устранение проблем

### Ошибка: "icon not found"

**Решение:** Иконки опциональны. electron-builder создаст стандартные. Или добавьте иконки в `build/`.

### Ошибка: "WASM file not found"

**Решение:** Проверьте, что `node_modules/sql.js/dist/sql-wasm.wasm` существует. Если нет, выполните `npm install`.

### Ошибка при сборке для другой платформы

**Решение:** 
- Для Windows нужна Windows машина или Wine
- Для macOS нужна macOS машина
- Для Linux нужна Linux машина

Или используйте CI/CD (GitHub Actions, etc.)

---

## Распространение

### Для пользователей

1. Загрузите установщики на файлообменник или GitHub Releases
2. Пользователи скачивают и устанавливают
3. Запускают через ярлык - всё работает!

### Автоматизация

Можно настроить автоматическую сборку через:
- GitHub Actions
- GitLab CI
- Jenkins
- И другие CI/CD системы

---

## Дополнительные настройки

### Подпись приложения (production)

Для production рекомендуется подписать приложение:

**Windows:**
- Authenticode certificate
- Настройте в `package.json` → `build.win.certificateFile`

**macOS:**
- Apple Developer ID
- Настройте в `package.json` → `build.mac.identity`

**Linux:**
- GPG подпись для репозиториев

### Автообновление

Можно настроить автообновление через:
- `electron-updater`
- Собственный сервер обновлений

---

## Чек-лист перед релизом

- [ ] Все зависимости установлены (`npm install`)
- [ ] Проект компилируется без ошибок (`npm run build`)
- [ ] Тестирование в режиме разработки (`npm run electron:dev`)
- [ ] Иконки добавлены (опционально, но рекомендуется)
- [ ] Версия обновлена в `package.json`
- [ ] Сборка установщика (`npm run dist`)
- [ ] Тестирование установщика на чистой системе
- [ ] Проверка ярлыков и запуска приложения
- [ ] Подготовка релиз-ноутов

---

**Готово! Теперь у вас есть установщик, который создаст ярлыки и позволит пользователям запускать приложение без консоли!** 🚀
