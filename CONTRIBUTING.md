# 🤝 Внесение вклада в Workspace Lite

Спасибо за интерес к развитию проекта! Этот документ поможет вам начать.

## 🎯 Текущие приоритеты

### MVP (версия 1.0) - Завершено ✅
- [x] Базовая структура Electron + React
- [x] SQLite база данных с FTS5
- [x] CRUD операции для страниц
- [x] Три типа страниц: Заметки, Задачи, Инструкции
- [x] Markdown редактор
- [x] Полнотекстовый поиск (Ctrl+K)
- [x] Теги и фильтрация
- [x] Экспорт в MD и CSV
- [x] Горячие клавиши
- [x] Production-ready сборка

### Версия 1.1 - Планируется
- [ ] Тёмная тема
- [ ] История версий страниц
- [ ] Drag & Drop для файлов
- [ ] Автосохранение
- [ ] Улучшенный Markdown (таблицы, синтаксис подсветка)
- [ ] Уведомления (toast messages)
- [ ] Импорт из Markdown файлов
- [ ] Резервное копирование БД

### Версия 1.2 - Будущее
- [ ] Связи между страницами (backlinks)
- [ ] Граф знаний (визуализация связей)
- [x] Шаблоны пользовательские ✅
- [ ] Календарь и канбан-доска для задач
- [ ] Статистика и аналитика
- [ ] Плагин система

### Версия 2.0 - Долгосрочно
- [ ] Локальная P2P синхронизация
- [ ] Коллаборация в локальной сети
- [ ] End-to-end шифрование
- [ ] Мобильная версия (React Native?)
- [ ] Web-версия (опционально)

## 🐛 Известные баги

### Критические
- Нет известных критических багов

### Средние
- [ ] При быстром переключении между страницами иногда не сохраняются изменения
- [ ] Markdown preview не обновляется в реальном времени
- [ ] Поиск не находит страницы с кириллическими тегами иногда

### Низкие
- [ ] Скроллбар в PageList иногда "прыгает"
- [ ] Фокус не всегда возвращается в textarea после форматирования
- [ ] Экспорт CSV не экранирует кавычки в некоторых случаях

## 💡 Предложения функций

Проголосуйте за нужные вам функции или предложите свои в Issues!

### Популярные запросы
1. **Вложения** - прикрепление файлов к страницам
2. **Рисование** - встроенный canvas для скетчей
3. **Шаблоны** - пользовательские шаблоны страниц
4. **Экспорт в PDF** - экспорт страниц в PDF
5. **Напоминания** - уведомления о дедлайнах задач

## 🚀 Как внести вклад

### 1. Настройка окружения

```bash
# Форкните репозиторий
git clone https://github.com/YOUR-USERNAME/workspace-lite.git
cd workspace-lite

# Установите зависимости
npm install

# Запустите в dev режиме
npm run electron:dev
```

### 2. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 3. Сделайте изменения

Следуйте стандартам кода:
- TypeScript strict mode
- ESLint правила (если настроены)
- Комментарии на русском для UI, на английском для кода
- Все UI-тексты на русском

### 4. Тестирование

```bash
# Проверьте, что приложение запускается
npm run electron:dev

# Проверьте сборку
npm run build

# Проверьте TypeScript
npx tsc --noEmit
```

**Минимальный чек-лист:**
- [ ] Приложение запускается без ошибок
- [ ] Основной функционал работает
- [ ] Нет console.error в DevTools
- [ ] Код соответствует стилю проекта

### 5. Коммит

```bash
git add .
git commit -m "feat: добавил тёмную тему"
# или
git commit -m "fix: исправил поиск по кириллице"
```

**Формат сообщений коммитов:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.п.

### 6. Push и Pull Request

```bash
git push origin feature/your-feature-name
```

Создайте Pull Request на GitHub с описанием:
- Что сделано
- Почему это нужно
- Как это работает
- Скриншоты (если UI изменения)

## 📋 Стандарты кода

### TypeScript

```typescript
// ✅ Хорошо
interface PageData {
  title: string;
  content: string;
}

function createPage(data: PageData): Page {
  // implementation
}

// ❌ Плохо
function createPage(data: any) {
  // implementation
}
```

### React компоненты

```tsx
// ✅ Хорошо - типизированные props
interface EditorProps {
  page: Page | null;
  onUpdate: (id: string, data: UpdatePageData) => void;
}

export default function Editor({ page, onUpdate }: EditorProps) {
  // implementation
}

// ❌ Плохо - без типов
export default function Editor(props) {
  // implementation
}
```

### Именование

- **Файлы**: PascalCase для компонентов (`Editor.tsx`), camelCase для утилит
- **Компоненты**: PascalCase (`PageList`)
- **Функции**: camelCase (`handleSave`)
- **Константы**: UPPER_SNAKE_CASE (`MAX_TAGS`)
- **Интерфейсы**: PascalCase (`Page`, `PageFilters`)

### Структура компонента

```tsx
import { useState, useEffect } from 'react';
import type { Page } from '../types';

interface ComponentProps {
  // props
}

export default function Component({ prop1, prop2 }: ComponentProps) {
  // State
  const [state, setState] = useState();
  
  // Effects
  useEffect(() => {
    // effect
  }, []);
  
  // Handlers
  const handleAction = () => {
    // logic
  };
  
  // Render
  return (
    <div>
      {/* JSX */}
    </div>
  );
}
```

## 🧪 Тестирование

### Запуск тестов (когда будут добавлены)

```bash
npm test              # Все тесты
npm test:watch        # Watch режим
npm test:coverage     # С покрытием
```

### Добавление тестов

```typescript
// __tests__/database.test.ts
import { describe, it, expect } from 'vitest';
import { createPage, getPage } from '../electron/database';

describe('Database', () => {
  it('should create and retrieve a page', () => {
    const page = createPage({
      type: 'note',
      title: 'Test',
      content: 'Content',
    });
    
    const retrieved = getPage(page.id);
    expect(retrieved?.title).toBe('Test');
  });
});
```

## 📝 Документация

При добавлении новой функции обновите:
- `README.md` - основное описание
- `CUSTOMIZATION.md` - если можно кастомизировать
- `ARCHITECTURE.md` - если меняется архитектура
- Inline комментарии в коде

## 🎨 UI/UX Guidelines

### Принципы дизайна

1. **Минимализм** - только необходимые элементы
2. **Скорость** - отзывчивый интерфейс
3. **Последовательность** - единый стиль
4. **Доступность** - понятные иконки и подсказки

### Цвета

```css
/* Основные */
--blue-600: #2563eb;      /* Акцент */
--gray-50: #f9fafb;       /* Фон */
--gray-900: #111827;      /* Текст */

/* Статусы */
--green-100: #dcfce7;     /* Успех */
--yellow-100: #fef3c7;    /* Предупреждение */
--red-100: #fee2e2;       /* Ошибка */
```

### Отступы

```css
/* Стандарты Tailwind */
p-2  /* 0.5rem - плотно */
p-4  /* 1rem - нормально */
p-8  /* 2rem - просторно */
```

## 🔍 Code Review процесс

Все Pull Requests проходят review:

**Что проверяется:**
- ✅ Код работает без ошибок
- ✅ Следует стандартам проекта
- ✅ Имеет понятные комментарии
- ✅ UI на русском языке
- ✅ TypeScript типы корректны
- ✅ Нет console.log в production коде

**Время review:** обычно 1-3 дня

## 🏆 Признание вклада

Все контрибьюторы будут добавлены в:
- `CONTRIBUTORS.md`
- Credits в приложении
- GitHub Contributors

## 📧 Контакты

- **Issues**: для багов и предложений
- **Discussions**: для обсуждений архитектуры
- **Email**: для приватных вопросов

## 📜 Лицензия

Внося вклад, вы соглашаетесь с MIT лицензией проекта.

---

**Спасибо за вклад в Workspace Lite! 🚀**