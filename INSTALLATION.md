# 📥 Установка Workspace Lite

## Для конечных пользователей

### Windows

#### Вариант 1: Установщик (рекомендуется) ✅

1. **Скачайте установщик**
   - Файл: `Workspace Lite-1.0.0-x64.exe` (или другой вариант)
   - Расположение: папка `release/` после сборки

2. **Запустите установщик**
   - Дважды кликните на `.exe` файл
   - Следуйте инструкциям мастера установки
   - Выберите папку установки (по умолчанию: `C:\Program Files\Workspace Lite`)

3. **Готово!**
   - Ярлык появится на рабочем столе
   - Ярлык появится в меню "Пуск" → "Workspace Lite"
   - Запустите приложение двойным кликом на ярлык

#### Вариант 2: Portable версия

1. **Скачайте portable версию**
   - Файл: `Workspace Lite-1.0.0-x64.exe` (portable)
   - Расположение: папка `release/` после сборки

2. **Запустите приложение**
   - Дважды кликните на `.exe` файл
   - Приложение запустится без установки
   - Все данные сохраняются рядом с исполняемым файлом

**Преимущества portable:**
- Не требует установки
- Можно переносить на USB
- Не оставляет следов в системе

---

### macOS

#### Вариант 1: DMG образ (рекомендуется) ✅

1. **Скачайте DMG образ**
   - Файл: `Workspace Lite-1.0.0.dmg` (или `Workspace Lite-1.0.0-arm64.dmg` для Apple Silicon)
   - Расположение: папка `release/` после сборки

2. **Установите приложение**
   - Откройте `.dmg` файл
   - Перетащите "Workspace Lite" в папку "Applications"
   - При первом запуске macOS может запросить разрешение (Системные настройки → Безопасность)

3. **Запустите приложение**
   - Откройте Launchpad или Finder → Applications
   - Найдите "Workspace Lite" и запустите
   - Для удобства можно закрепить в Dock

#### Вариант 2: ZIP архив

1. **Скачайте ZIP архив**
   - Файл: `Workspace Lite-1.0.0-mac.zip`
   - Расположение: папка `release/` после сборки

2. **Распакуйте и запустите**
   - Распакуйте архив
   - Переместите "Workspace Lite.app" в папку Applications
   - Запустите приложение

---

### Linux

#### Вариант 1: AppImage (рекомендуется) ✅

1. **Скачайте AppImage**
   - Файл: `Workspace Lite-1.0.0-x64.AppImage`
   - Расположение: папка `release/` после сборки

2. **Сделайте исполняемым**
   ```bash
   chmod +x "Workspace Lite-1.0.0-x64.AppImage"
   ```

3. **Запустите приложение**
   ```bash
   ./Workspace\ Lite-1.0.0-x64.AppImage
   ```

4. **Создайте ярлык (опционально)**
   - AppImage автоматически создаст ярлык в меню приложений
   - Или создайте `.desktop` файл вручную

#### Вариант 2: DEB пакет (Ubuntu/Debian)

1. **Скачайте DEB пакет**
   - Файл: `Workspace Lite_1.0.0_amd64.deb`
   - Расположение: папка `release/` после сборки

2. **Установите через терминал**
   ```bash
   sudo dpkg -i "Workspace Lite_1.0.0_amd64.deb"
   ```

   Или через графический установщик:
   - Дважды кликните на `.deb` файл
   - Следуйте инструкциям установщика

3. **Запустите приложение**
   - Найдите "Workspace Lite" в меню приложений
   - Или выполните: `workspace-lite`

---

## 🚀 Для разработчиков (сборка установщика)

### Требования

- Node.js 18+
- npm или yarn
- Все зависимости установлены (`npm install`)

### Сборка установщика

#### Windows

```bash
# Сборка для Windows
npm run dist
```

**Результат:**
- `release/Workspace Lite Setup 1.0.0.exe` - установщик NSIS
- `release/Workspace Lite-1.0.0-x64.exe` - portable версия

#### macOS

```bash
# Сборка для macOS
npm run dist
```

**Результат:**
- `release/Workspace Lite-1.0.0.dmg` - DMG образ
- `release/Workspace Lite-1.0.0-mac.zip` - ZIP архив

#### Linux

```bash
# Сборка для Linux
npm run dist
```

**Результат:**
- `release/Workspace Lite-1.0.0-x64.AppImage` - AppImage
- `release/Workspace Lite_1.0.0_amd64.deb` - DEB пакет

### Добавление иконок

Перед сборкой добавьте иконки в папку `build/`:

- **Windows**: `build/icon.ico` (256x256, многоразмерная)
- **macOS**: `build/icon.icns`
- **Linux**: `build/icon.png` (512x512 или 1024x1024)

См. `build/README.md` для подробных инструкций.

---

## 📍 Расположение данных после установки

После установки данные приложения хранятся в стандартных папках:

### Windows
```
%APPDATA%\workspace-lite\workspace.db
C:\Users\[ВашеИмя]\AppData\Roaming\workspace-lite\workspace.db
```

### macOS
```
~/Library/Application Support/workspace-lite/workspace.db
```

### Linux
```
~/.config/workspace-lite/workspace.db
```

---

## 🔄 Обновление приложения

### Windows (NSIS установщик)

1. Скачайте новую версию установщика
2. Запустите установщик
3. Он автоматически удалит старую версию и установит новую
4. Ваши данные сохранятся (они хранятся отдельно от приложения)

### macOS

1. Скачайте новую версию DMG
2. Перетащите новую версию в Applications (заменит старую)
3. Ваши данные сохранятся

### Linux

1. Установите новый DEB пакет или AppImage
2. Замените старую версию
3. Ваши данные сохранятся

---

## ❓ Устранение проблем

### Windows: "Приложение не может быть запущено"

**Решение:**
1. Проверьте, что скачали правильную версию (x64 или x32)
2. Убедитесь, что у вас установлены Visual C++ Redistributables
3. Попробуйте запустить от имени администратора

### macOS: "Приложение повреждено"

**Решение:**
1. Откройте Системные настройки → Безопасность
2. Нажмите "Открыть в любом случае" рядом с предупреждением
3. Или выполните в терминале:
   ```bash
   xattr -cr /Applications/Workspace\ Lite.app
   ```

### Linux: "Нет прав на выполнение"

**Решение:**
```bash
chmod +x "Workspace Lite-1.0.0-x64.AppImage"
```

---

## 🎯 Что происходит при установке

### Windows (NSIS)

1. ✅ Создается папка установки (по умолчанию: `C:\Program Files\Workspace Lite`)
2. ✅ Копируются файлы приложения
3. ✅ Создается ярлык на рабочем столе
4. ✅ Создается ярлык в меню "Пуск"
5. ✅ Регистрируется в "Установка и удаление программ"
6. ✅ Опционально: создается автозапуск (если выбран)

### macOS (DMG)

1. ✅ Приложение копируется в `/Applications`
2. ✅ Создается ярлык в Launchpad
3. ✅ Можно закрепить в Dock

### Linux (DEB)

1. ✅ Устанавливается в `/opt/workspace-lite` или `/usr/local`
2. ✅ Создается `.desktop` файл для меню приложений
3. ✅ Регистрируется в системе

---

## 💡 Советы

### Автозапуск (Windows)

После установки можно настроить автозапуск:
1. Откройте "Диспетчер задач" → "Автозагрузка"
2. Найдите "Workspace Lite" и включите

### Ярлыки

- **Windows**: Ярлыки создаются автоматически
- **macOS**: Перетащите приложение из Applications в Dock
- **Linux**: Ярлык появляется в меню приложений автоматически

### Перенос данных

Если нужно перенести данные на другой компьютер:
1. Найдите файл `workspace.db` (см. "Расположение данных")
2. Скопируйте его на новый компьютер
3. Поместите в ту же папку на новом компьютере

---

**Готово! Приятной работы с Workspace Lite!** 🚀
